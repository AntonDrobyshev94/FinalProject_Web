@{
    ViewData["Title"] = "Изменение проекта";
    Layout = "_Layout";
}
<link href="~/css/IndexStyle.css" rel="stylesheet" />
@using FinalProject_Web.Model;
@{
    bool isAdmin;
    if (ViewBag.IsAdmin != null && ViewBag.IsAdmin == true)
    {
        isAdmin = true;
    }
    else
    {
        isAdmin = false;
    }
}
@model ProjectModel
@if (!isAdmin)
{
    <div class="form-container">

        <div class="request-form center">
            <div>
                <form action="~/Web/ChangeProject" method="post" enctype="multipart/form-data" id="uploadForm">
                    <input type="file" name="imageFile" accept="image/*" id="uploadInput"/>
                    <input type="hidden" name="imageName" value="@Model.ImageName">
                    <input type="hidden" name="id" value="@Model.Id">
                    <label>Название проекта</label>
                    <input type="text" name="name" value="@Model.Name" /><br />
                    <label>Описание проекта</label><br />
                    <textarea name="description" rows="3" class="description">@Model.Description</textarea><br /><br />
                    <p><input type="submit" style="margin-top:20px" class="styled-button" value="Подтвердить изменения" /></p>
                </form>
            </div>
        </div>
    </div>
}

